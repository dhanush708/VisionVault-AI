[project]
name = "visionvault-ai-backend"
version = "1.0.0"
description = "VisionVault AI - Enterprise CCTV Video Storage Optimization Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
